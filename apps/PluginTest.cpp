//#include "Board.hpp"
//#include "PluginLoader.hpp"

#include <iostream>

int main(int argc, char** argv)
{
  /*FlakedTuna::PluginLoader loader;
  loader.FindPluginsAtDirectory("../bin/lib", "so");  // Windows
  std::vector<std::shared_ptr<Board>> plugins = loader.BuildAndResolvePlugin<Board>();

  for(unsigned int i = 0; i != plugins.size(); ++i) std::cout << plugins[i]->getType() << "  " << plugins[i]->getName() << std::endl;

  return 0;*/
}
